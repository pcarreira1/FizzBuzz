{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Fizz buzz lambda handler",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "test:coverage": "jest --coverage",
		"functional:tests": "jest ./test",
		"compile": "npm test && npm run functional:tests && npm run build"
  },
  "author": "Pedro Carreira",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.86",
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.11",
    "aws-sdk": "^2.1044.0",
    "jest": "^27.4.3",
    "ts-jest": "^27.1.0",
    "typescript": "^4.5.2"
  }
}
